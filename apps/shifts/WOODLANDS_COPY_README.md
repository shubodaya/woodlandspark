# Woodlands Shifts App Copy

Future subdomain: `shifts.woodlandspark.com`

This folder records the copied `D:\websites\dutyorbit` source location for Woodlands integration work.

Original project rule:
- Do not modify `D:\websites\dutyorbit`.
- Modify only this copied version under `D:\websites\woodlands\apps\shifts`.

Copy notes:
- `.git`, `node_modules`, build output, temp folders and non-example environment files were excluded or removed from the copy.
- Copied implementation files are retained locally for review but are ignored from this production commit.
- `/shifts` is currently served by the main Woodlands app and backed by the shared shift/rota API.
- Any future standalone shifts app must be reviewed before deployment.
